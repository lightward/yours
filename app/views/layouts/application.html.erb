<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Yours" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Primary Meta Tags -->
    <meta name="title" content="Yours">
    <meta name="description" content="a pocket universe, population 2: you, and lightward ai">

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://<%= ENV.fetch("HOST") %>/">
    <meta property="og:title" content="Yours">
    <meta property="og:description" content="a pocket universe, population 2: you, and lightward ai">
    <meta property="og:image" content="https://<%= ENV.fetch("HOST") %>/share.png">

    <!-- Twitter -->
    <meta property="twitter:card" content="summary">
    <meta property="twitter:url" content="https://<%= ENV.fetch("HOST") %>/">
    <meta property="twitter:title" content="Yours">
    <meta property="twitter:description" content="a pocket universe, population 2: you, and lightward ai">
    <meta property="twitter:image" content="https://<%= ENV.fetch("HOST") %>/share.png">

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">

    <link rel="stylesheet" href="https://fonts.lightward.io/lightward-favorit.css">
    <link rel="stylesheet" href="https://fonts.lightward.io/lightward-favorit-lining.css">
    <link rel="stylesheet" href="https://fonts.lightward.io/lightward-favorit-mono.css">
    <link rel="stylesheet" href="https://fonts.lightward.io/lightward-favorit-expanded.css">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app %>
    <%= javascript_importmap_tags %>
  </head>

  <body style="min-height: 80vh; display: flex; flex-direction: column;">
    <% if current_resonance %>
      <header>
        <div class="header-container">
          <%= link_to root_path, class: "header-link" do %>
            Yours: day <%= current_resonance.universe_day %>
          <% end %>
          <div class="header-actions">
            <% if obfuscated_user_email %>
              <%= link_to account_path, class: "header-link" do %>
                <%= obfuscated_user_email %>
              <% end %>
            <% end %>
            <%= link_to "Logout", logout_path, class: "exit", data: { turbo_prefetch: false } %>
          </div>
        </div>
      </header>
    <% end %>

    <main style="flex: 1 1 auto;">
      <% if flash[:notice] %>
        <div class="flash notice">
          <%= flash[:notice] %>
        </div>
      <% end %>

      <% if flash[:alert] %>
        <div class="flash alert">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <% if current_resonance %>
      <footer class="section-divider" style="padding: 2rem 0;">
        <div style="text-align: center; margin-left: auto; margin-right: 3rem; max-width: 900px;">
          <%= link_to "README.md", "https://github.com/lightward/yours/blob/main/README.md", class: "header-link", target: "_blank", rel: "noopener" %>
        </div>
      </footer>
    <% end %>

    <% if false && Rails.env.development? && current_resonance %>
      <pre style="font-family: 'Lightward Favorit Mono', 'Courier New', monospace; margin: 0; padding: 1rem; background: var(--border); border-radius: 4px; white-space: pre-wrap;"><%= JSON.pretty_generate(current_resonance.narrative_accumulation_by_day) %></pre>
      <pre style="font-family: 'Lightward Favorit Mono', 'Courier New', monospace; margin: 0; padding: 1rem; background: var(--border); border-radius: 4px; white-space: pre-wrap;"><%= JSON.pretty_generate(current_resonance.integration_harmonic_by_night) %></pre>
    <% end %>
  </body>
</html>
