<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Yours" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">

    <link rel="stylesheet" href="https://fonts.lightward.io/lightward-favorit.css">
    <link rel="stylesheet" href="https://fonts.lightward.io/lightward-favorit-lining.css">
    <link rel="stylesheet" href="https://fonts.lightward.io/lightward-favorit-mono.css">
    <link rel="stylesheet" href="https://fonts.lightward.io/lightward-favorit-expanded.css">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if current_resonance %>
      <header>
        <div style="display: flex; justify-content: space-between; align-items: center;">
          <%= link_to root_path, style: "color: var(--accent); text-decoration: none; transition: all 0.2s; font-family: 'Lightward Favorit Mono', monospace; font-size: 0.9em;" do %>
            Yours: day <%= current_resonance.universe_day %>
          <% end %>
          <div style="display: flex; align-items: center; gap: 1rem;">
            <% if obfuscated_user_email %>
              <%= link_to account_path, style: "color: var(--accent); text-decoration: none; transition: all 0.2s; font-family: 'Lightward Favorit Mono', monospace; font-size: 0.9em;" do %>
                <%= obfuscated_user_email %>
              <% end %>
            <% end %>
            <%= link_to "Logout", logout_path, class: "secondary", style: "text-decoration: none;", data: { turbo_prefetch: false } %>
          </div>
        </div>
      </header>
    <% end %>

    <main>
      <% if flash[:notice] %>
        <div style="padding: 1rem; background: var(--border-light); color: var(--foreground); border-left: 4px solid var(--success); margin-bottom: 1rem; border-radius: 4px;">
          <%= flash[:notice] %>
        </div>
      <% end %>

      <% if flash[:alert] %>
        <div style="padding: 1rem; background: var(--border-light); color: var(--foreground); border-left: 4px solid var(--error); margin-bottom: 1rem; border-radius: 4px;">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <% if Rails.env.development? && current_resonance %>
      <pre style="font-family: 'Lightward Favorit Mono', 'Courier New', monospace; margin: 0; padding: 1rem; background: var(--border); border-radius: 4px; white-space: pre-wrap;"><%= JSON.pretty_generate(current_resonance.narrative_accumulation_by_day) %></pre>
      <pre style="font-family: 'Lightward Favorit Mono', 'Courier New', monospace; margin: 0; padding: 1rem; background: var(--border); border-radius: 4px; white-space: pre-wrap;"><%= JSON.pretty_generate(current_resonance.integration_harmonic_by_night) %></pre>
    <% end %>
  </body>
</html>
